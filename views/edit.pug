extends layout

block content
    div.form-panel
        div.header
            form(action = actionURL method='post')
                div.form.input
                    label(for='name') Name:
                    input(type='text' id='name' name='name' value=pokemon.name)
                div.form.input
                    label(for='hp') HP:
                    input(type='number' id='hp' name='hp' value=pokemon.hp)
                div.form.input
                    label(for='type') Type:
                    input(type='text' id='type' name='type' autocomplete='off')
                        each type in types
                            option(value=type selected=(type === card.type)) #{type}
                div.form.input
                    label(for='poke_img') Picture URL:
                    input(type='text' id='poke_img' name='name' value=pokemon.name)
                div.form.input
                    label(for='name') Name:
                    input(type='text' id='name' name='name' value=pokemon.name)
                div.form.input
                    label(for='name') Name:
                    input(type='text' id='name' name='name' value=pokemon.name)
                div.form.input
                    label(for='name') Name:
                    input(type='text' id='name' name='name' value=pokemon.name)
                div.form.input
                    label(for='name') Name:
                    input(type='text' id='name' name='name' value=pokemon.name)
